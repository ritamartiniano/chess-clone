#include "MoveSearch.h"


void MoveSearch::StartSearch(std::vector<Move>& possible_moves, int depth, PieceTeam team)
{
    // Mov
    // for (const auto& move : possible_moves)
    // {
    //     
    // }
}

int MoveSearch::AlphaBetaMax(int alpha, int beta, int depth)
{
    // if(depth  == 0)
    // {
    //     return Evaluate
    // }

    return 0;
}
